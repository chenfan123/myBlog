services:
  postgres:
    image: postgres:18-alpine
    container_name: myblog-postgres
    environment:
      POSTGRES_DB: myblog
      POSTGRES_USER: myblog
      POSTGRES_PASSWORD: myblog_local
    ports:
      - "5432:5432"
    volumes:
      - myblog_postgres_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U myblog -d myblog"]
      interval: 5s
      timeout: 3s
      retries: 10

  redis:
    image: redis:latest
    container_name: myblog-redis
    restart: unless-stopped
    ports:
      - "6380:6379"
    command: redis-server --appendonly yes
    volumes:
      - myblog_redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  agent:
    build:
      context: ./apps/agent
      args:
        PIP_INDEX_URL: ${PIP_INDEX_URL:-https://mirrors.cloud.tencent.com/pypi/simple}
    env_file:
      - ./apps/agent/.env.local
    environment:
      DATABASE_URL: postgresql+psycopg://myblog:myblog_local@postgres:5432/myblog
      MILVUS_URI: ${MILVUS_URI:-http://host.docker.internal:19530}
    ports:
      - "8002:8002"
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  a2ui-server:
    build:
      context: ./apps/a2ui
      dockerfile: Dockerfile.server
    env_file:
      - ./apps/agent/.env.local
    environment:
      PORT: 3000
      DASHSCOPE_MODEL2: ${DASHSCOPE_MODEL2:-qwen3.8-flash}
      A2UI_SSE_INTERVAL_MS: ${A2UI_SSE_INTERVAL_MS:-160}
      A2UI_MAX_TOKENS: ${A2UI_MAX_TOKENS:-8192}
    ports:
      - "3001:3000"
    restart: unless-stopped

  a2ui-web:
    build:
      context: ./apps/a2ui
      dockerfile: Dockerfile.web
    ports:
      - "5173:80"
    depends_on:
      - a2ui-server
    restart: unless-stopped

volumes:
  myblog_postgres_data:
  myblog_redis_data:
