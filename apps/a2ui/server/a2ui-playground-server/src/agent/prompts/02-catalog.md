# 材料：组件 catalog 与字段约定

下面是 docs/supported-a2ui-standard-components-v0_8.md。只使用其中的组件键名和字段形状。
禁止：Heading、Button.label、Button.text、Image.source.uri、Card.children、Button.children、action.context 写成 {}。

{{catalogMaterial}}
